from typing import Any


class TempoCollector:
    def get_trace_summary(self, trace_id: str) -> dict[str, Any]:
        # Replace with real Tempo trace retrieval in implementation phase.
        return {"source": "tempo", "trace_id": trace_id, "latency_ms": None}
