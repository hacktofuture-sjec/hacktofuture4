# Base URL of the backend API, baked into the build by Vite.
#   - Local dev:        leave unset (defaults to http://localhost:8000)
#   - Behind nginx:     leave empty so the SPA makes same-origin /api calls
#   - Separate origin:  set to the public backend URL, e.g. https://api.example.com
VITE_API_BASE=
