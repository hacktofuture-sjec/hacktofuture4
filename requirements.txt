# Workspace Root Requirements
# Generated per user request for unified "most stable" module definitions

# Linting / Code Quality
black==24.1.1
flake8==7.0.0

# Backend (Django)
django==4.2
djangorestframework==3.14.0
psycopg2-binary>=2.9.9  # Relaxed pin to allow newer wheels if available
redis==5.0.1
django-cors-headers==4.3.1
celery==5.3.4

# Agent Service (FastAPI + LangGraph)
fastapi>=0.109.0
uvicorn[standard]==0.24.0
langgraph>=0.1.0        # Bumped to support langchain-core 0.2+
langchain-openai>=0.1.0
langchain>=0.2.0
sse-starlette>=2.0.0
httpx==0.27.0
pydantic==2.11.0

# General Server / Configuration
python-dotenv==1.0.0

# MCP Servers (Jira / HubSpot)
fastmcp==0.1.0
hubspot-api-client==8.2.0