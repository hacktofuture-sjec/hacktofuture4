# Shared Contracts

This folder is the only integration boundary between frontend and backend.

## Rule
- Any payload shape change must be committed here first.
- Update `chat.contract.json` version for every breaking or additive contract change.
