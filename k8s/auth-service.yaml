apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  type: NodePort
  selector:
    app: auth-service
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30011
      protocol: TCP
