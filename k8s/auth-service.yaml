---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: prod
  labels:
    app: auth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
    spec:
      volumes:
        - name: app-script
          configMap:
            name: sample-app-script
        - name: otel-deps
          emptyDir: {}
      initContainers:
        - name: install-otel
          image: python:3.11-slim
          command:
            - sh
            - -c
            - pip install --no-cache-dir --target /otel-deps opentelemetry-sdk opentelemetry-exporter-otlp-proto-http
          volumeMounts:
            - name: otel-deps
              mountPath: /otel-deps
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
      containers:
        - name: auth-service
          image: python:3.11-slim
          command:
            - sh
            - -c
            - python /app/app.py
          ports:
            - containerPort: 8081
          volumeMounts:
            - name: app-script
              mountPath: /app
            - name: otel-deps
              mountPath: /otel-deps
          resources:
            requests:
              memory: "32Mi"
              cpu: "25m"
            limits:
              memory: "64Mi"
              cpu: "100m"
          env:
            - name: SERVICE_NAME
              value: auth-service
            - name: PORT
              value: "8081"
            - name: PYTHONPATH
              value: /otel-deps
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://tempo.monitoring.svc.cluster.local:4318/v1/traces
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: prod
spec:
  selector:
    app: auth-service
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
