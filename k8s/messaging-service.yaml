apiVersion: v1
kind: Service
metadata:
  name: messaging-service
spec:
  type: NodePort
  selector:
    app: messaging-service
  ports:
    - port: 3002
      targetPort: 3002
      nodePort: 30012
      protocol: TCP
