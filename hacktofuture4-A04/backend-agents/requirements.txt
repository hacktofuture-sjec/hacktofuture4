# ─── Core framework ───────────────────────────────────────────────────────────
google-adk>=0.3.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.7.0

# ─── OpenTelemetry SDK (instrument your own services to feed Tempo + Prometheus) ──
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-grpc>=1.24.0   # send traces to Tempo via OTLP
opentelemetry-exporter-prometheus>=0.45b0          # expose /metrics for Prometheus scrape
opentelemetry-instrumentation-fastapi>=0.45b0      # auto-instrument this FastAPI app
opentelemetry-instrumentation-httpx>=0.45b0        # trace outbound HTTP calls to tools

# ─── Prometheus client (expose metrics from this app itself) ──────────────────
prometheus-client>=0.20.0

# ─── Grafana / Loki log shipping (optional — use if pushing logs to Loki directly) ─
python-logging-loki>=0.3.1     # logging handler that ships to Loki push API
